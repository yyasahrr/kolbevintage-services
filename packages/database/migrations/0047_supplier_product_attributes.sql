/*
  چرخهٔ عمرِ محصولِ تأمین‌کننده: مقصدِ کانونیکِ ویژگی‌های سطحِ محصول.

  چرا: `category.attributes_schema` از ابتدا وجود داشت تا هر دسته طرحِ ویژگی‌های
  خودش را اعلام کند و `product_variant.attributes` هم همین شکل را دارد، اما
  مقصدِ مقادیرِ سطحِ محصول هرگز ساخته نشده بود. در نتیجه
  `supplier_product_submission.attributes` پذیرفته و در بازبینیِ ادمین نمایش
  داده می‌شد و سپس هنگامِ تأیید بی‌صدا دور ریخته می‌شد.

  فقط-روبه‌جلو و غیرمخرب: ستون با مقدارِ پیش‌فرضِ `'{}'::jsonb` و NOT NULL اضافه
  می‌شود، پس ردیف‌های موجود دست‌نخورده و معتبر می‌مانند و هیچ داده‌ای پاک نمی‌شود.
*/

ALTER TABLE "product" ADD COLUMN "attributes" jsonb DEFAULT '{}'::jsonb NOT NULL;
