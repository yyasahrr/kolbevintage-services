# رودمپ حداقلی پلتفرم کلبه

> این سند جایگزین پرسشنامههای `docs/` نیست؛ خروجی فشرده تصمیمهای گرفتهشده برای دو سهگانه «اکنون / مهاجرت / رشد» است. پرسشنامهها بهعنوان چکلیست کشف نیازمندی باقی میمانند و پاسخهای تکمیلشده به همین سند ارجاع داده میشوند.

## اهداف سنجشپذیر (۱۲ ماه)

1. نگهداری بک‌اند یکپارچه Next.js و PostgreSQL بدون موتور تجارت ثالث
2. چکاوت واقعی خردهفروشی: سفارش ثبتشده در دیتابیس + پرداخت درگاهی + موجودی واقعی
3. کاتالوگ دیتامحور (خروج از فایل هاردکد `src/data/catalog.ts` و ادیتور localStorage)
4. آمادگی بار ۱۰۰ هزار کاربر: LCP < ۲.۵ ثانیه، جستجوی سمت سرور، پایش دسترسپذیری

## اکنون (فاز ۰ — همین شاخه)

- [x] انتقال API، احراز هویت و ماژول‌های دامنه به Next.js + PostgreSQL
- [x] رفع خطاهای بلعیدهشده پنل ساپلایر (بنر خطا + تلاش دوباره) — ممیزی P2
- [x] پینکردن وابستگیهای `latest` پنل ساپلایر — ممیزی P2
- [x] میزبانی محلی فونت وزیرمتن (حذف وابستگی به Google Fonts)

## مهاجرت (فاز ۱ تا ۵)

- [x] **اجرای کامل در یک موج:** API یکپارچه با ماژول‌های دامنه و PostgreSQL
- [x] کاتالوگ عمده/موجودی روی بک‌اند؛ VIP و پنل ساپلایر به API داخلی وصل شدند
- [x] ماژول PO + گردش تأمین کامل (confirmed → preparing → shipped → delivered)
- [x] چکاوت خرده واقعی شد (ماژول `retail`) — ایراد P0 «چکاوت دمویی» بسته شد
- [x] خاموشی Supabase: حذف `supabase-js`، حذف پوشه `supabase/`، bundle فروشگاه ~۵۰٪ سبک شد

## رشد (پس از مهاجرت)

- [ ] اتصال واقعی زرینپال (فعلاً پرووایدر sandbox است) + webhook تأیید پرداخت
- [ ] کاتالوگ خرده هم دیتامحور شود (فعلاً فقط عمده از بک‌اند میآید؛ `src/data/catalog.ts` هنوز هاردکد است)
- [ ] جستجوی محصولات سمت سرور و کش/CDN
- [ ] شکستن کامپوننتهای غولپیکر (`ProductPage` ۱۰۰۶ خط، `Admin` ۸۹۲ خط) — ممیزی P1
- [ ] code splitting واقعی (نیازمند بازنگری `vite-plugin-singlefile`) — ممیزی P2
- [ ] CI/CD و مانیتورینگ (هنوز وجود ندارد!)

## تصمیمهای باز (نیازمند مالک محصول)

1. اولویت نهایی خرده در برابر عمده در سه ماه اول پس از مهاجرت
2. انتخاب درگاه پرداخت و قرارداد آن
3. زیرساخت میزبانی (سرور داخل ایران یا ابر خارجی) و بودجه زیرساخت
4. سرنوشت `vite-plugin-singlefile` برای فروشگاه (تعارض با code splitting)
