# Phase 5.13 Full Backend Adversarial Audit

Start SHA: `b269b0fe34bc1838aca79f4f9e2eb3f2d6531bf6`

This document is the live evidence register for the final backend gate. Findings are classified only as **CONFIRMED DEFECT**, **HARDENING GAP**, **DOCUMENTED ACCEPTED RISK**, or **FALSE POSITIVE**. A confirmed defect is not closed until its reproduction, root cause, minimal fix, and regression test are recorded.

## Evidence register

| ID | Classification / severity | Domain | Attack or failure scenario | Reproduction and result | Fix checkpoint | Regression evidence | Residual risk |
|---|---|---|---|---|---|---|---|
| A-001 | CONFIRMED DEFECT / high | Supplier tenancy | An authenticated non-admin guesses a supplier ID and reads its member roster; the same caller can create a supplier authority row. | Source inspection reproduced both routes with default authentication only: `GET /suppliers/:id/members` and `POST /suppliers` had neither role nor ownership checks. The member query filters only by attacker-controlled `supplierId`. | A: restrict both surfaces to admin role plus `wholesale:membership:view/manage`. | `phase-5-13-authz-adversarial.test.ts` pins both role and permission gates. | Supplier self-service membership remains available only through tenant-aware supplier-team surfaces. |
| A-002 | CONFIRMED DEFECT / high | Admin authorization | A restricted admin invokes Phase 5.12 compatibility writers because they checked the broad `admin` role but did not invoke granular RBAC. | Source reproduction found ungated writes in operational logs, CMS settings, supplier decisions, VIP account decisions, RFQ creation, bulk pricing, and catalog moderation. A restricted admin session therefore reached the service layer. | A: add `AdminPermissionGuard` and an existing catalog permission matched to each operation. | `phase-5-13-authz-adversarial.test.ts` verifies denial semantics and pins every compatibility writer permission. | The permission catalog remains intentionally coarse for compatibility endpoints; operations inherit the closest established domain permission. |
| A-003 | FALSE POSITIVE / none | CMS preview | Public preview routes could appear to expose unpublished CMS data. | Each preview route verifies a short-lived HMAC token bound to both target type and revision ID using timing-safe comparison, and returns 404 on failure. | None. | Existing CMS preview tests plus source verification in `cms-preview.service.ts` and `cms-public.controller.ts`. | Possession of a valid preview URL grants access until its bounded expiry, by design. |
| A-004 | CONFIRMED DEFECT / high — CLOSED | Orders/VIP domain boundary | GitHub Actions run `36019931474` failed `architecture-freeze.test.ts` because `vip.service.ts` directly read the Orders-owned `wholesale_order_request` table during request conversion. | The VIP service imported `wholesaleOrderRequest` to detect an existing link and to validate the newly created link, crossing the ownership boundary and coupling VIP to Orders persistence. | A: Orders now checks existing links, creates and validates request-to-order links through `OrdersRepository`, then invokes the VIP-owned accepted-to-ordered transition in the same transaction. VIP no longer imports or queries the link table. | `architecture-freeze.test.ts`, `phase-5-13-orders-vip-boundary.test.ts`, and the existing Phase 4.3/4.3.1 conversion, concurrency, rollback, replay, version, expiry, hash, and account-isolation cases passed. GitHub Actions run `36063833627` succeeded: API 128 files / 1,385 tests; repository 176 files / 1,756 tests. | Database uniqueness on `wholesale_order_request.request_id`, deterministic request/account locks, and the encompassing transaction remain authoritative against duplicate conversion and orphan state. |
| B-001 | HARDENING GAP / medium — CLOSED | Settlement monetary input | An authenticated administrator submits decimal, signed, non-numeric, or over-limit money to hold, commission-policy, or shipping-economics writers. | Source-level reproduction proved the controllers called `BigInt(body.*)` directly. Malformed values escaped as generic conversion errors; a negative hold amount was silently converted by the service into an unbounded hold (`null`) instead of being rejected. | B: one strict decimal-string parser now bounds all three inputs to `0..MAX_MONEY`, requires a positive amount when a bounded hold is requested, and returns `INVALID_AMOUNT`/400 before any service mutation. | `phase-5-13-b-financial-input.test.ts` covers decimals, signs, exponent form, text, overflow, policy money, shipping money, and verifies zero service calls on rejection. GitHub Actions run `36066787677` succeeded. | Database money CHECK constraints remain defense in depth; controller validation now gives stable client semantics before persistence. |
| C-001 | HARDENING GAP / high — FIXED, CI PENDING | Audit / observability | A module passes an untrusted nested token, password, authorization value, or provider error to the audit or logging boundary. | Source proof showed `AuditService.record` persisted `before`, `after`, and `metadata` verbatim. `redactSensitive(Error)` returned the original error message and stack, and normalized lookup keys did not match entries such as `api_key` and `set-cookie`. | C: redact all flexible audit payloads at the append-only sink, normalize the sensitive-key catalog, recursively sanitize Error messages/stacks, and preserve Date values used by legitimate audit evidence. | `phase-5-13-c-data-safety.test.ts` proves nested audit redaction, Error message/stack redaction, normalized key matching, safe-field retention, and Date preservation. Existing Phase 4.9 security and observability suites passed locally. Full CI pending. | Structured free text can only be redacted by known credential patterns; callers must still avoid placing arbitrary secret formats in narrative fields. |
| C-002 | CONFIRMED DEFECT / medium — FIXED, CI PENDING | Support attachment confidentiality | A customer, VIP, or supplier reads a support conversation containing an attachment and receives the private storage `objectKey` in the API response. | `listMessages` returned raw `support_attachment` rows. The portal controllers serialize that service result directly, so the object key crossed the private storage boundary even though file bytes were not public. | C: map attachment rows to an explicit response projection and omit `objectKey`. | The real-PostgreSQL `phase-5-2-support-conversation.test.ts` suite passes 7/7 and now asserts that portal-visible attachment metadata has no `objectKey`. Full CI pending. | Attachment upload/download transport is not wired in this repository; no claim is made for an external object-storage provider or malware scanner. |
| C-003 | FALSE POSITIVE / none | Support attachment access descriptor | The service method named `getAttachmentAccess` appears to issue a signed, expiring file capability. | Route inventory proved there is no support attachment upload or download controller and no token-consumption path. The method returns an in-memory UUID and timestamp only, so it cannot currently expose file bytes. | None; adding a storage/token protocol would invent functionality outside this audit. | Source and controller-route inventory. | A future implementation must bind a verifiable capability to attachment, actor/case authorization, expiry, and scan status before exposing a download route. |

## Baseline evidence

- Continuation branch and local/remote head matched the required start SHA.
- Migration verification: 47/47 migrations, 199 tables, 461 foreign keys, 589 CHECK constraints.
- Preflight, typecheck, package build, infrastructure verification, and production Next build passed before modification.
- The full local suite cannot bind its hard-coded secondary PostgreSQL port in this Windows sandbox (`EACCES` on `127.0.0.1:55432`). The existing PostgreSQL verification instance on port 54329 is healthy; Linux GitHub Actions remains the required full-suite gate.

The register will be extended at each checkpoint with executed evidence, commit SHA, CI run, and remaining risk.

## CI-recovery local regression evidence

- The wholesale conversion suites passed locally: 24/24 tests across `orders-phase-4-3.test.ts` and `orders-phase-4-3-1.test.ts`, including single conversion, concurrent idempotency, link correctness, rollback/orphan protection, replay, version, expiry, hash, and account/buyer isolation.
- The previously suspected advisory-lock case, `guarantees single-worker execution under heavy concurrent attempts`, reproduced in 4/4 isolated Windows runs (no randomized seed): `executed.length` was 2 instead of 1. No retry, sleep, assertion weakening, or unrelated fix is included in this CI-recovery change; GitHub Actions remains the cross-platform gate.
- The previously suspected notification-cleanup case, `C27 isolates notification failure from commerce truth`, was **NOT REPRODUCED** in 3/3 isolated runs.

## Checkpoint B adversarial evidence

- Authoritative money remains BIGINT/string-based and server-derived. The audit re-ran payment amount/currency verification, duplicate provider events, refund allocation/over-refund protection, concurrent refund completion, settlement double-spend, post-payout refund recovery, payout evidence, retail last-stock concurrency, checkout replay, promotion cap concurrency, failed-order rollback, and immutable promotion attribution.
- Local real-PostgreSQL result: 63/63 selected financial/concurrency tests passed across payment provider, payment/refund concurrency, settlement adversarial, payout/withdrawal, retail order concurrency, and retail promotions suites. No migration or ownership change was required.
- B-001 is the only actionable Checkpoint B finding. Other challenged invariants were supported by row locks, idempotency hashes/keys, unique constraints, append-only ledgers, bounded CHECK constraints, and the encompassing transaction tests.
- Checkpoint B commit `1d92a569ef740a114e54be7390602dc6d214e8b2` passed GitHub Actions run `36066787677`.

## Checkpoint C adversarial evidence

- Auth/session replay, stale token version, suspended account, role spoofing, malformed claims, admin permissions, and cross-tenant order/support/compliance/analytics access were re-exercised through the established security suites.
- Compliance signed access remains target-bound and expiring; CMS public media validates MIME signatures and uses a separate public storage port; analytics exports enforce owner scope, expiry, bounded rows, and CSV formula neutralization.
- Notification outbox/provider/preference/domain-wiring suites passed 66/66 locally, confirming provider failure isolation, claim/retry behavior, receipt recording, preference suppression, and durable commerce-event wiring.
- Local selected result: 187 passing assertions across Checkpoint C data-safety, Phase 4.9 security/observability/adversarial (excluding the already documented Windows-only advisory-lock mismatch), support, compliance, notifications, analytics/export, CMS source-level security, and authorization suites. The existing Windows path-separator discrepancy in the CMS local-storage test and the advisory-lock observation were not changed; Linux GitHub Actions is the authoritative platform gate.
- C-001 and C-002 are the actionable findings. No auth model, storage provider, notification provider, or scheduler behavior was redesigned.
